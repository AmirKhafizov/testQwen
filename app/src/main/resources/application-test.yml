spring:
  datasource:
    url: jdbc:tc:postgresql:16:///cost_monitor
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    com.construction.costmonitor: DEBUG
